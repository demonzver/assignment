star_threshold: 10000       # minimal stars a repo must have
new_limit_per_topic: 25      # how many *new* repos per keyword per run

topics:                      # list of keywords / topics
  - spark
  - python
